import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    viewportWrapper: {
        display: 'flex',
        position: 'relative',
        width: '100%',
        height: '100%',
        backgroundColor: 'red',
    },
    modalWrapper: {
        
    }
});

export default styles;
